/*!
 * 默认与预理器
 * 所有的表达式，在其他预处理器处理完毕后，会被该预处理器处理
 */

define( function ( require, exports, module ) {

    /**
     * 处理函数
     * @param {String} data 输入表达式
     * @return {String} 处理过后的表达式
     */
    return function ( data ) {

        return data;

    };

} );
