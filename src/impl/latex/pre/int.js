/**
 * 积分格式预处理器
 */

define( function () {

    return function ( input ) {

        input.replace( /\\(i+)nt\\b/g, function ( match, intStr, suffix ) {

            debugger;
            return

        } );

    };

} );
